[project]
name = "do-ob-io"
version = "0.1.0"
description = "Workspace for do-ob uv Python projects"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = ["python/*"]
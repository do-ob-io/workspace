services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
    image: do-ob_devcontainer:latest
    volumes:
      - ..:/workspace:cached
    working_dir: /workspace
    command: sleep infinity
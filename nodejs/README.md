# NPM Packages and Applications

This directory contains Node.js projects included in the workspace.

## Usage

Clone a Node.js project repository into this folder then install all npm dependencies from the root of the workspace with `pnpm install`.